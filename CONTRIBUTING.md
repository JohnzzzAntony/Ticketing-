# Contributing

1. Create a feature branch.
2. Keep changes focused and covered by tests where practical.
3. Run `npm run verify` before opening a pull request.
4. Include migration and seed updates when changing Prisma models.
5. Do not commit `.env`, local databases, logs, or generated build output.
